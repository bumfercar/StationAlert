name: Cross-platform CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  offline-tests:
    name: Python 3.11 / ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Install uv and Python 3.11
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          python-version: "3.11"
          enable-cache: true

      - name: Sync locked dependencies
        run: uv sync --frozen --extra dev

      - name: Install FFmpeg on Linux
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install --yes ffmpeg

      - name: Install FFmpeg on macOS
        if: runner.os == 'macOS'
        run: brew install ffmpeg

      - name: Install FFmpeg on Windows
        if: runner.os == 'Windows'
        run: choco install ffmpeg --no-progress --yes

      - name: Verify FFmpeg
        run: ffmpeg -version

      - name: Run FFmpeg integration tests
        run: uv run --frozen pytest tests/audio

      - name: Run evaluation unit tests
        run: uv run --frozen pytest tests/evaluation

      - name: Run RTZR unit tests
        run: uv run --frozen pytest tests/rtzr

      - name: Check root CLI help
        run: uv run --frozen pytest tests/test_cli.py::test_help_describes_project

      - name: Check Streaming CLI help
        run: uv run --frozen pytest tests/test_cli.py::test_stream_file_help_makes_cost_and_privacy_controls_visible

      - name: Check Batch CLI help
        run: uv run --frozen pytest tests/test_cli.py::test_batch_file_help_exposes_model_domain_and_private_output

      - name: Check review CLI help
        run: uv run --frozen pytest tests/test_cli.py::test_prepare_review_help_exposes_fixed_chunks_and_private_output

      - name: Check evaluation CLI help
        run: uv run --frozen pytest tests/test_cli.py::test_evaluate_run_help_exposes_private_inputs_and_system_name

      - name: Check Batch alignment CLI help
        run: uv run --frozen pytest tests/test_cli.py::test_prepare_batch_predictions_help_exposes_alignment_inputs

      - name: Run CLI behavior tests
        run: uv run --frozen pytest tests/test_cli.py -k "not help"

      - name: Run domain unit tests
        run: uv run --frozen pytest tests/test_detection.py

      - name: Run Ruff
        run: uv run --frozen ruff check .
